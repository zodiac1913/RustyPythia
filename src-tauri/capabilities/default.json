{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and any additional workspace windows",
  "windows": ["main", "workspace-*"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
